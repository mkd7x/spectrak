# Spectrak Improvement Backlog

This folder contains follow-up work identified while reviewing the Spectrak experiment and the `todo-list-test` consuming project.

## Priority Order

1. Add immutable spec revisions and historical retrieval.
2. Add reference validation for indexes and source-code markers.
3. Add batch retrieval and spec discovery endpoints.
4. Add optimistic concurrency for updates.
5. Add export and local snapshot support.
6. Add authentication, authorization, and audit logging for shared deployments.

The detailed items are in `recommended-improvements.md`.
